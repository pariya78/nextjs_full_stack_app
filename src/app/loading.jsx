//when switching between pages we see this loading instead of a blank white page
import React from 'react';

function Loading(props) {
    return (
        <div>Loading ...</div>
    );
}

export default Loading;